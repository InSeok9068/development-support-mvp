{
  "name": "@apps/kjca",
  "type": "module",
  "scripts": {
    "test": "vitest --run --environment jsdom",
    "test:ui": "vitest --ui",
    "dev": "vite",
    "build": "vue-tsc && vite build --emptyOutDir",
    "preview": "vite preview",
    "type-check": "vue-tsc --noEmit",
    "typegen": "pocketbase-typegen -j ./pb_schema.json -o ./src/api/pocketbase-types.ts",
    "lint": "oxlint && eslint --cache",
    "lint:fix": "oxlint --fix && eslint --fix",
    "lint:format": "oxfmt",
    "pb:dev": "pnpm run pb:hook && pnpm exec dotenv -e ../../.env -- cmd /d /c \"cd /d %USERPROFILE%\\IdeaProjects\\pocketbase\\kjca && .\\pbw.exe pocketbase serve --dev\"",
    "pb:hook": "cmd /d /c \"robocopy .\\pb_hooks %USERPROFILE%\\IdeaProjects\\pocketbase\\kjca\\pb_hooks /MIR >NUL & if errorlevel 8 exit /b 1 & exit /b 0\""
  },
  "dependencies": {
    "@packages": "workspace:*"
  }
}

